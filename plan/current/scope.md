---
title: "Scope - 0000001-doc-nav"
---
# Scope - 0000001-doc-nav

## In Scope
- `src/web-app/docs.manifest.json` — new file, defines page order and titles
- `scripts/build-docs.js` — extended to read manifest, inject top nav links, inject prev/next nav
- `src/web-app/index.html` — CTA link fixed from `p001-planifest-master-plan.html` to `01-overview.html`
- `src/web-app/src/style.css` — add `.doc-pagination` styles for prev/next nav block

## Out of Scope
- `sitemap.html` — no changes
- Mobile hamburger menu — no changes to hamburger behaviour
- Search / full-text index — not in this release
- `dist/` files — never patched directly; regenerated by build

## Deferred
- **Active-page highlight in top nav** — requires per-page class or runtime JS to mark the current page; deferred to a follow-up fast-path change. Blocked until: team decides between static per-page class injection vs. runtime `location.pathname` check.
- **Doc versioning** — already deferred in component.yml
- **Interactive playground** — already deferred in component.yml
